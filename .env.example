# AI Employee Vault — Environment Variables
# Copy this file to .env and fill in your values.
# .env is gitignored and must never be committed.

# ── General ───────────────────────────────────────────────────────────────────

# Root path to the vault (leave blank to use the default: parent of Scripts/)
VAULT_PATH=

# Set to "false" to allow watchers to write files. Default is "true" (safe mode).
DRY_RUN=true

# ── Gmail Watcher ─────────────────────────────────────────────────────────────

# Path to credentials.json downloaded from Google Cloud Console
GMAIL_CREDENTIALS_PATH=Scripts/credentials.json

# Path where the OAuth token will be saved after running gmail_auth.py
GMAIL_TOKEN_PATH=Scripts/token.json

# Gmail search query used to filter incoming messages
GMAIL_QUERY=is:unread is:important

# Seconds between Gmail polls (default: 120)
GMAIL_CHECK_INTERVAL=120

# ── WhatsApp Watcher ───────────────────────────────────────────────────────────

# Directory where Playwright saves the WhatsApp Web browser session (QR scan).
# This folder is gitignored — it contains auth cookies. Delete it to re-scan QR.
WHATSAPP_SESSION_PATH=Scripts/.whatsapp_session

# Comma-separated keywords that trigger an action file (case-insensitive).
# Leave blank to capture ALL unread messages.
WHATSAPP_KEYWORDS=urgent,asap,invoice,payment,help,question,project

# Seconds between WhatsApp polls (default: 30)
WHATSAPP_CHECK_INTERVAL=30

# ── Discord Watcher ────────────────────────────────────────────────────────────

# Bot token from Discord Developer Portal → Bot → Token (required)
# Never commit this value.
DISCORD_BOT_TOKEN=

# Comma-separated channel snowflake IDs where @mentions are watched.
# Right-click a channel → Copy ID. Leave blank = watch all channels for mentions.
DISCORD_CHANNEL_IDS=

# Monitor DMs sent directly to the bot (default: true)
# DMs always trigger — no @mention needed.
DISCORD_MONITOR_DMS=true

# ── LinkedIn Watcher & Poster ──────────────────────────────────────────────────

# OAuth 2.0 Client credentials from LinkedIn Developer Portal
# Create app at https://developer.linkedin.com/apps
# Required products: "Sign In with LinkedIn using OpenID Connect" + "Share on LinkedIn"
LINKEDIN_CLIENT_ID=
LINKEDIN_CLIENT_SECRET=

# Access token and person URN obtained by running: uv run python Scripts/linkedin_auth.py
# Never commit these values.
LINKEDIN_ACCESS_TOKEN=
LINKEDIN_PERSON_URN=

# Directory where Playwright saves the LinkedIn session (login cookies).
# This folder is gitignored — delete it to force re-login.
LINKEDIN_SESSION_PATH=Scripts/.linkedin_session

# Seconds between LinkedIn message polls (default: 300 = 5 minutes)
LINKEDIN_CHECK_INTERVAL=300

# Days of the week to trigger the linkedin-posting skill (comma-separated).
# Used by the orchestrator. Leave blank to disable scheduled posting.
LINKEDIN_POST_SCHEDULE=MON,WED,FRI
